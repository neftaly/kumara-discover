import tape from 'tape';
// import discover from '../src';

tape.test('Discovery', test => {
  test.plan(1);
  test.skip('todo');
  test.end();
});

// discover().map(
//   state => state.toJS()
// ).map(
//   state => JSON.stringify(state, null, 2)
// ).map(
//   state => console.warn('\n###', state)
// );
